switch -> compare it to a postal office-ish
switch knows the IP & MAC addresses of all the computers and which port they're connected to
I have manually added the IP addresses & network masks to the computers